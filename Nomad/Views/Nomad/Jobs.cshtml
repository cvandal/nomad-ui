@model Nomad.Extensions.PaginatedList<Nomad.Models.Job>

@{
    var prevDisabled = !Model.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.HasNextPage ? "disabled" : "";
}

<div class="row">
    <div class="col-md-12">
        <form class="search" method="get">
            <h4>Jobs</h4>

            <input name="search" type="text" placeholder="&#8981;">
        </form>

        <table class="table table-hover">
            <thead>
                <tr class="row">
                    <th class="col">ID</th>
                    <th class="col">Name</th>
                    <th class="col">Type</th>
                    <th class="col">Priority</th>
                    <th class="col">Status</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var job in Model)
                {
                    <tr class="row">
                        <td class="col"><a href="/job?id=@job.ID">@job.ID</a></td>
                        <td class="col">@job.Name</td>
                        <td class="col">@job.Type</td>
                        <td class="col">@job.Priority</td>
                        <td class="col">@job.Status</td>
                    </tr>
                }
            </tbody>
        </table>

        <nav>
            <ul class="pagination">
                <li class="page-item @prevDisabled"><a class="page-link" href="/jobs?page=@(Model.PageIndex - 1)">Previous</a></li>
                <li class="page-item @nextDisabled"><a class="page-link" href="/jobs?page=@(Model.PageIndex + 1)">Next</a></li>
            </ul>
        </nav>
    </div>
</div>
